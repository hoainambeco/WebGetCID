<html lang="en">
<head>
    <title>NAMNHPH14161</title>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script
            src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
            crossorigin="anonymous"
    ></script>
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
            crossorigin="anonymous"
    />
    {{#if loginCSS}}
        <link rel="stylesheet" href="{{loginCSS}}" />
    {{/if}}
    {{#if userCSS}}
        <link rel="stylesheet" href="{{userCSS}}" />
    {{/if}}
    {{#if homeJS}}
        <!--   Core JS Files   -->
        <script src="{{homeJS}}"></script>
        <script src="{{homeJS1}}"></script>
        <script src="{{homeJS2}}"></script>
        <script src="{{homeJS3}}"></script>
        <!-- Chart JS -->
        <script src="{{homeJS4}}"></script>
        <!--  Notifications Plugin    -->
        <script src="{{homeJS5}}"></script>
        <!-- Control Center for Black Dashboard: parallax effects, scripts for the example pages etc -->
        <script
                src="{{homeJS6}}"
        ></script><!-- Black Dashboard DEMO methods, don't include it in your project! -->
        <script src="{{homeJS7}}"></script>
    {{/if}}
    {{#if homeCSS}}
        <link rel="stylesheet" href="{{homeCSS}}" />
        <link
                href="https://fonts.googleapis.com/css?family=Poppins:200,300,400,600,700,800"
                rel="stylesheet"
        />
        <link
                href="https://use.fontawesome.com/releases/v5.0.6/css/all.css"
                rel="stylesheet"
        />
        <!-- CSS Files -->
        <link href="{{css}}" rel="stylesheet" />
        <!-- CSS Just for demo purpose, don't include it in your project -->
        <link href="{{demo}}" rel="stylesheet" />
    {{/if}}
</head>
<body>
{{{body}}}

</body>
</html>
